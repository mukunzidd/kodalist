<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>" style="border: 2px dashed white; margin: 15px 0  10px 0; color: white; background-color: teal">KODALIST</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <div class="row">
        <div class="col-md-3"></div> 
        <div class="col-md-6">
          <ul class="nav navbar-nav navicons">
            <% if user_signed_in? %>
              <li><a href="<%= root_path %>"><i class="fa fa-comments-o"></i><img class="navimg" src="<%= image_url("forum.png") %>" alt=""></span>  <br> FORUM</a></li>
              <li><a href="<%= conversations_path %>"><span><i class="fa fa-comments-o"></i><img class="navimg" src="<%= image_url("empty-message.png") %>" alt=""></span> <br>MESSAGES</a></li>
              <li><a href="<%= finder_path %>"> <i class="fa fa-github"></i><img class="navimg" src="<%= image_url("search.png") %>" alt=""></span>  <br><small>GitHub</small>FINDER</a></li>
            <% end %>
          </ul>
        </div> 
        <div>
          <ul class="nav navbar-nav navbar-right">
            <% if user_signed_in? %>
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><i class="fa fa-user-o"> </i>  <%= current_user.username %>  <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                        <li class="nav-item">
                        <%= link_to 'Account Settings', edit_user_registration_path, class: "nav-link" %>
                        </li>
                        <li role="separator" class="divider"></li>
                        <li class="nav-item">
                          <%#= link_to 'Profile', class: "nav-link" %>
                        </li>
                        <li role="separator" class="divider"></li>
                        <li class="nav-item">
                          <%= link_to 'Log Out', destroy_user_session_path, class: "nav-link", method: :delete %>
                        </li>
                      <% else %>
                        
                        <%= link_to 'Sign In', new_user_session_path, class: "nav-link btn button w3-margin" %>
                        <span class="w3-text-teal">  OR  </span>
                        <%= link_to 'Sign Up', new_user_registration_path, class: "nav-link btn button w3-margin", method: :get %>
                </ul>
              </li>
            <% end %>
          </ul>
        </div>        
      </div>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>