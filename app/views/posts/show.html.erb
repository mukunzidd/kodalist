<h1><%= @post.title %></h1>

<p><em><%= @post.user.email %> | posted <%= @post.created_at %></em></p>
<p><%= @post.body %></p>
<%= link_to :Delete, post_path, method: :delete, remote: true, data: { confirm: 'Are you for real?'} %>
<%= link_to :Edit, edit_post_path %>
<hr>
<h3>Post A Reply:</h3>
<%= render "relies/comments" %>
<br>
<%= link_to :Back, root_path %>
